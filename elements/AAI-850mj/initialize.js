function(instance, context) {
    // Initialize state
    instance.data.jsonObject = {};
    instance.data.mergeString = "";
    
    // Define state to hold the resulting merged string
    instance.data.resultingString = "";
}
